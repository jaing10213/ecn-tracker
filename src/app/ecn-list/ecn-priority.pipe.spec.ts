import { EcnPriorityPipe } from './ecn-priority.pipe';

describe('EcnPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new EcnPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
