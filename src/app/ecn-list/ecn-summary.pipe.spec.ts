import { EcnSummaryPipe } from './ecn-summary.pipe';

describe('EcnSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new EcnSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
