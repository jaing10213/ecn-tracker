import { EcnFilterPipe } from './ecn-filter.pipe';

describe('EcnFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EcnFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
