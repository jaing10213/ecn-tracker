import { EcnStatusPipe } from './ecn-status.pipe';

describe('EcnStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new EcnStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
