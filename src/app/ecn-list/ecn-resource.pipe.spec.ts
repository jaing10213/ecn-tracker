import { EcnResourcePipe } from './ecn-resource.pipe';

describe('EcnResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new EcnResourcePipe();
    expect(pipe).toBeTruthy();
  });
});
