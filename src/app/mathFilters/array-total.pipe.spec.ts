import { ArrayTotalPipe } from './array-total.pipe';

describe('ArrayTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
